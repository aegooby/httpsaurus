
interface Node { id: ID! }

type Error { message: String! }

type Post implements Node
{
    id: ID!
    text: String!
}

interface UserPayload implements Node
{
    id: ID!
    email: String!
    receipt: String
    password: String!
}
interface UserJWT
{
    id: ID!
    email: String!
    receipt: String
}
type User implements Node & UserJWT
{
    id: ID!
    email: String!
    receipt: String
}

type Query 
{ 
    node(id: ID!): Node

    readUser(id: ID!): User
    readCurrentUser: User
}
type Mutation
{
    createUser(email: String!, password: String!): User!
    loginUser(email: String!, password: String!): String!
    logoutUser: Boolean!
    revokeUser(id: ID!): Boolean!
}
